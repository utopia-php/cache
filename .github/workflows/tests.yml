name: Tests

on: [push]

jobs:
  tests: 
    name: Tests
    runs-on: ubuntu-20.04

    steps:
      - name: Clone Repository
        uses: actions/checkout@v3

      - name: Setup Docker Container
        run: |
            docker-compose up -d
            sleep 5

      - name: Composer Install
        run: |
            docker run --rm --volume "$(pwd)":/app composer install --ignore-platform-reqs --optimize-autoloader --no-plugins --no-scripts --prefer-dist
      
      - name: Run Tests
        run: |                
            docker ps
            docker-compose exec -T php8 vendor/bin/psalm --show-info=true
            docker-compose exec -T php8 vendor/bin/phpunit --configuration phpunit.xml